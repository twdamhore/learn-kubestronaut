{
  "set": "01",
  "round": 15,
  "total_reviewed": 100,
  "issues_found": 2,
  "issues": [
    {
      "id": "s01-q033",
      "type": "wrong_explanation",
      "severity": "moderate",
      "what": "Explanation labels for options B and C are swapped",
      "detail": "In the 'Why other options are wrong' section, the explanation for B says 'Bundling environment-specific config into the image means rebuilding for each environment' — but option B is 'Use a centralized configuration server that the application queries at startup for its settings'. Meanwhile, the explanation for C says 'While configuration servers exist, the twelve-factor methodology specifically advocates for environment-based configuration, not centralized servers' — but option C is 'Bundle environment-specific configuration files into the container image at build time per stage'. The B and C explanations have been transposed.",
      "suggestion": "Swap the labels so that B explains why a centralized configuration server is wrong ('While configuration servers exist, the twelve-factor methodology specifically advocates for environment-based configuration, not centralized servers') and C explains why bundling config in images is wrong ('Bundling environment-specific config into the image means rebuilding for each environment')."
    },
    {
      "id": "s01-q018",
      "type": "giveaway",
      "severity": "moderate",
      "what": "Question stem contains the word 'sidecar' which is the correct answer",
      "detail": "The question stem reads 'a logging sidecar that reads the application's log files from a shared volume'. The correct answer (option A) is 'The sidecar pattern'. The word 'sidecar' in the stem directly reveals the answer without requiring the student to identify the pattern from the described behavior. The diagram giveaway from round 14 was fixed (renamed to 'Helper Container'), but the stem itself still contains the giveaway.",
      "suggestion": "Replace 'a logging sidecar' in the stem with a neutral description such as 'a helper container' or 'a secondary container'. For example: 'A developer defines a Pod with two containers: an application container and a helper container that reads the application's log files from a shared volume.'"
    }
  ]
}
